# Finetuning LLaVA-Med

## How to run

1. Clone this repository <https://github.com/haotian-liu/LLaVA> into project root folder.
2. Change directory to LLava and `pip install -e .`. This will build the llava package.
3. Go to `notebooks` directory and run all cells of the notebook
